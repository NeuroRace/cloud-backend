# secrets — nunca commitar
.env
supabase/functions/.env

# supabase local
supabase/.temp/
supabase/.branches/

# os
.DS_Store

# secret de producao (local, nunca commitar)
.secret.prod.env

# scratch de agentes (nunca commitar)
.superpowers/
artifacts/
inbox/
